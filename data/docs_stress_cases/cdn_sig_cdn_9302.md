# CDN-9302 Origin Shield Timeout Signature

Signature: `CDN-9302`.

For origin shield timeout, inspect shield region, origin latency, keepalive reuse,
connection limit, DNS resolution, and whether retries concentrate traffic on one
origin cluster.

