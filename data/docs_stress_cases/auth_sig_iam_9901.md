# IAM-9901 Token Audience Signature

Signature: `IAM-9901`.

For token audience mismatch, inspect issuer, audience, resource server id, gateway
auth plugin configuration, and whether the client used a token minted for another
environment.

