# CDN-9301 Stale Cache Signature

Signature: `CDN-9301`.

For stale CDN response after release, inspect cache key, purge scope, origin header,
surrogate key, and whether HTML and API responses use different cache-control policies.

