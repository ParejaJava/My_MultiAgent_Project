# VDB-7101 Empty Retrieval Signature

Signature: `VDB-7101`.

When retrieval returns empty documents, verify ingestion count, collection name,
persist directory, metadata filter, query text, and whether the configured embedding
model matches the stored vector collection.

