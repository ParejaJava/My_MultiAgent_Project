# CDN-9303 Signed URL Expiry Signature

Signature: `CDN-9303`.

For signed URL expiry complaints, compare client clock, token TTL, URL generation time,
edge cache behavior, and whether redirects preserve the signature query string.

