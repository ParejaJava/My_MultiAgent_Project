# VDB-7103 Vector Dimension Signature

Signature: `VDB-7103`.

For invalid vector dimension, compare the embedding model used during ingestion with
the model used during query. Rebuild the Chroma collection after changing embedding
dimension or provider.

